# Environment Variables Example
# Copy this file to .env if you need custom configuration

# Server Port (default: 3000)
PORT=3000

# Node Environment (development or production)
NODE_ENV=production

# Optional: Custom Bare Server Path
# BARE_PATH=/bare/
