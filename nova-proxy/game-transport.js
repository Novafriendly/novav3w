// Serve packaged HTML through Scramjet; external assets use libcurl/Wisp.
function novaGameTransport(transport){const request=transport.request.bind(transport);transport.request=async(remote,method,body,headers,signal)=>{if(remote.origin!=='https://nova-games.invalid')return request(remote,method,body,headers,signal);if(!/^\/(math-tutors-main|html-main\/html-main|assets-main\/assets-main|covers-main\/covers-main|Img)\//.test(remote.pathname))throw Error('Unknown local game path.');const response=await fetch(new URL(remote.pathname+remote.search,location.origin),{method,body,signal,redirect:'error',credentials:'omit',headers:headers.filter(([name])=>['range','accept','content-type'].includes(name.toLowerCase()))});const resultHeaders=new Headers(response.headers);if(/\.html(?:-[a-z]+)?$/i.test(remote.pathname))resultHeaders.set('Content-Type','text/html; charset=utf-8');return {body:response.body,headers:[...resultHeaders],status:response.status,statusText:response.statusText};};return transport;}
